# ROOT-to-CSV

This script can convert traccc ROOT files to CSV files for easier parsing.
Not for direct use, but is designed to be used with the physics plot
generator.

To make this code, simply run:

```bash
$ make
```
