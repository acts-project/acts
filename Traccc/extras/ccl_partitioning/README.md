# CCL Partitioning

This small utility allows us to analyse the efficiency of different bin
packing schemes for the partitioning of cells for the CCL algorithm.

To run:

```
stack run cells.csv
```

Where `cells.csv` is any traccc-compatible cell input file.
