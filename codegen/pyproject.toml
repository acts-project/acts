[project]
name = "codegen"
version = "0.3.0"
description = "Shared sympy-based C++ code generation core for ACTS and detray"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "sympy"
]

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"
