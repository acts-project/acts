[pytest]
# The source tree holds a 'detray' package next to the test directories, which
# would shadow the compiled one if the test paths were prepended to sys.path.
addopts = --import-mode=importlib
testpaths =
    python/Io/tests
    python/Plugins/tests
    python/Tests/tests
